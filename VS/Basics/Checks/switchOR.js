function switchCase (input){
    let name = `Harry` ;

    switch (name) {
        case `Kiro`: 
        case `Harry`: 
        case `Stephen` : console.log (`Admin`) ; break ;
        default: console.log (`${name} is breaking in!`) ; break ;
    }
}

switchCase () 