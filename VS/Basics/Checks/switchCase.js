function switchCase (input){
    let name = `Dimo` ;

    switch (name) {
        case `Kiro`: console.log (`Kiro is Here!`) ; break ;
        case `Harry`: console.log (`Harry rocks!`) ; break ;
        default: console.log (`${name} is breaking in!`) ; break ;
    }
}

switchCase () 